pipeline
{
    agent any
    stages
    {
        stage('scm checkout')
        {
        steps
        {
           git branch: 'master' git url: 'https://github.com/tiwariak1992/git-test.git'
           }
        }
    }
}
